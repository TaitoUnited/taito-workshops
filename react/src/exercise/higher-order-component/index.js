import React from 'react';

/* TODO:
 * Implement a HOC that updates the `document.title` when the component mounts
 * based on provided title string and then resets it back to what it was
 * when the component unmounts.
 */

function withDocumentTitle(Comp) {
  // TODO: implement
  return Comp;
}

export default withDocumentTitle;
