import React from 'react';
import styled from 'styled-components';

/* TODO:
 * Implement a Tabs compound component that renders tab buttons and
 * the selected tab content according to the Usage component.
 *
 * -> tab title comes from the `label` prop
 * -> tab content is the children of the tab panel static component
 * -> selected tab is changed when a tab button is clicked
 * -> OPTIONAL: allow setting the initial tab via props
 * -> OPTIONAL: allow setting the selected tab via props
 *
 * Styling the tabs is optional but try to make the tabs look like proper tabs
 * eg. Bootstrap tabs or Material design tabs ;)
 */

class Tabs extends React.Component {
  render() {
    return (
      <div>TODO</div>
    );
  }
}

export default Tabs;
